"use client";

/**
 * Legacy re-export — Passport credentials now render from backend records.
 * Prefer importing PassportCredentials directly.
 */
export {
  PassportCredentials,
  CredentialCard,
} from "@/components/passport/PassportCredentials";
